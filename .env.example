# Star Spreader Configuration
# Copy this file to .env if you need to override default settings

# Authentication:
# Star-spreader uses Databricks Unified Authentication with profile support.
# Authenticate using the Databricks CLI:
#   databricks auth login                    # Creates DEFAULT profile
#   databricks auth login --profile prod     # Creates named profile
#
# Profiles are stored in ~/.databrickscfg and contain workspace URL and credentials.
# Use the --profile CLI flag to select a profile (defaults to DEFAULT).

# Optional: SQL warehouse ID for functional tests
# Only needed if you're running functional tests that execute queries
# DATABRICKS_WAREHOUSE_ID=abc123xyz
