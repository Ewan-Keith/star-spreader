# Databricks Configuration
# Copy this file to .env and fill in your values

# Authentication:
# Star-spreader uses Databricks Unified Authentication which automatically
# discovers credentials from your local environment:
# - Databricks CLI auth (`databricks auth login`) - Recommended
# - Azure CLI authentication
# - Environment variables (DATABRICKS_HOST, DATABRICKS_TOKEN)
# - Configuration file (~/.databrickscfg)
#
# No configuration is needed if you're already authenticated via databricks CLI.
# Simply run: databricks auth login

# Optional: Specify a particular Databricks workspace host
# (Leave commented to use the default from your databricks CLI profile)
# DATABRICKS_HOST=https://your-workspace.cloud.databricks.com

# Optional: SQL warehouse for query validation
# DATABRICKS_WAREHOUSE_ID=abc123xyz

# Optional: Default catalog and schema (defaults shown)
# DATABRICKS_CATALOG=main
# DATABRICKS_SCHEMA=default
